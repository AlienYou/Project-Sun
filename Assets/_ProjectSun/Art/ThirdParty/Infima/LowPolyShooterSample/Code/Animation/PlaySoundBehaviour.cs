using UnityEngine;

namespace ProjectSun.FPS.ThirdParty.InfimaSample
{
    /// <summary>
    /// Compatibility placeholder for a state behaviour embedded in a copied sample controller.
    /// Project Sun owns presentation audio, so the sample package runtime is never invoked here.
    /// </summary>
    public class PlaySoundBehaviour : StateMachineBehaviour
    {
    }
}
