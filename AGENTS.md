# Project Sun 开发规范

## 注释要求

- 每次新增或修改代码时，所有具有业务含义、状态转换、数学计算、兼容处理、性能优化或非显然行为的处理逻辑都必须添加清晰的中文注释，说明其目的、执行方式以及必要的边界条件或特殊情况。简单赋值、清晰的空值判断和能够由命名完整表达意图的代码无需逐行重复注释。
- 所有参数都必须添加中文注释，说明参数的用途、含义、单位（如适用）、有效范围、默认行为以及特殊取值（如适用）。参数包括但不限于方法参数、构造函数参数、配置项、序列化字段和公开字段。
- 公共方法的参数应使用 XML `<param>` 注释；Inspector 字段应使用中文注释并配合 `[Tooltip]`；私有辅助方法的参数在语义不明显或具有单位、范围、特殊值时必须添加注释。
- 注释必须描述设计意图和使用约束，不能只复述代码名称或语法。
- 修改已有逻辑或参数时，必须同步检查并更新相关注释，确保注释与实际行为一致。
- 对简单但不可省略的逻辑，也应提供简洁注释；避免无意义、过时或与实现不一致的注释。

## 工程与资源安全

- 本项目固定使用 Unity `2022.3.51f1c1`；未经明确要求，不得升级 Unity 版本、URP、Input System 或其他 Package。
- 不得手动修改 `Library`、`Temp`、`Logs`、`obj`、自动生成的 `.csproj` 和 `.sln` 文件。
- 新增、移动、重命名或删除 Unity 资源时，必须同时正确处理对应的 `.meta` 文件，避免 GUID 丢失和资源引用断裂。
- 不得直接修改第三方原始资源。正式使用或需要调整的资源应复制到 `Assets/_ProjectSun` 下，并保留来源及许可证记录。
- 修改场景、Prefab、材质、Shader、ScriptableObject 或渲染配置前，必须确认不会破坏现有序列化引用。
- 重命名已有序列化字段时，必须使用 `FormerlySerializedAs` 保留旧数据兼容；不得仅修改字段名称导致场景、Prefab 或资产数据丢失。

## 代码结构

- 运行时代码放在 `Assets/Scripts` 的对应功能目录中；仅编辑器使用的代码放在 `Assets/Editor`，不得让运行时代码依赖 `UnityEditor`。
- 新增运行时代码应使用 `ProjectSun.FPS.*` 命名空间，并按 Core、Input、Player、Weapons、Rounds、AI、Presentation、UI、Bootstrap 等现有职责划分。
- 保持玩法逻辑、表现逻辑和编辑器工具之间的职责分离，避免 UI、动画或渲染代码直接成为战斗规则权威。
- `RoundManager` 负责回合和比赛规则；武器、伤害、配装及战术装备不得复制一套独立的回合状态判断。
- 优先扩展现有组件、配置资产和接口；新增并行系统前，必须确认现有系统无法合理复用。
- 新增公开 API、类型、事件和序列化字段时，必须说明其职责、生命周期、调用方以及空值或失败行为。

## Unity 生命周期与引用

- 必须明确组件初始化、启用、禁用和销毁阶段的行为；事件订阅与取消订阅必须成对出现。
- 必需组件应通过 `[RequireComponent]`、启动时校验或清晰的错误日志进行约束，不得依赖未说明的场景隐式配置。
- 对可能为空的场景引用、Prefab 引用和 ScriptableObject 配置必须进行防御性检查，并提供可定位问题的错误信息。
- 除 Bootstrap、场景安装器或明确的兼容路径外，避免在高频逻辑中使用 `FindObjectOfType`、`GameObject.Find`、`Resources.Load` 等全局查找。
- 编辑器工具修改场景、Prefab 或资产时，应支持 Undo、标脏和保存流程，避免产生无法撤销或未持久化的修改。

## 性能要求

- 不得在 `Update`、`FixedUpdate`、渲染回调、射击检测和 AI 高频循环中引入不必要的堆内存分配。
- 高频物理查询优先使用 NonAlloc API，并复用缓冲区；不得无界创建临时集合、材质、纹理、Render Texture 或 GameObject。
- 运行时创建的材质、纹理、Render Texture、事件订阅和临时对象必须在适当生命周期中释放或回收。
- 新增倍镜渲染、后处理、AI 搜索或物理检测时，必须说明启用条件、更新频率和性能影响。
- 不得为了隐藏配置错误而在每帧自动搜索、创建或修复对象。

## 功能契约

- 弹道命中仍以相机中心射线确定瞄准目标，并由枪口射线验证物理路径；不得让瞄具模型或 HUD 准星直接决定伤害结果。
- `SOCKET_Scope`、`Model`、`AimAnchor`、`LensAnchor` 和 `AttachmentCalibrationRoot` 的现有职责必须保持分离。
- 修改武器或附件系统时，必须验证腰射、ADS、换枪、换弹、Clip Probe、运行时装配及瞄具渲染路径。
- 回合逻辑必须保持未来服务器权威迁移的可能性，避免由 HUD、动画或客户端表现直接修改权威状态。
- 开发测试快捷键和自动修复逻辑不得静默进入正式玩法路径，必须具有明确的测试开关。

## 验证要求

- 修改 C# 代码后，必须至少确认项目能够通过编译，并检查新增的 Unity Console 错误和警告。
- 修改场景、Prefab、武器或附件资产后，必须执行与改动相关的运行时验证，不能只确认编译成功。
- 修复缺陷时，应验证触发问题的原始路径，并检查相关边界情况，避免仅处理表面现象。
- 新增纯逻辑代码时，应优先补充 EditMode 测试；涉及组件生命周期、场景或输入交互时，应考虑 PlayMode 测试。
- 无法在当前环境中完成的验证必须明确记录，不能将“未验证”描述为“已完成”。

## 文档同步

- 功能范围、优先级或完成状态发生变化时，必须同步更新 `Docs/ProjectSun工程开发计划.md`。
- 修改武器、附件、ADS、瞄具或校准工作流时，必须同步检查对应的 Authoring 文档和生产规范。
- 新增配置字段时，除代码注释外，还应通过 `[Tooltip]`、自定义 Inspector 或相关文档向 Unity 编辑器使用者说明其用途。
